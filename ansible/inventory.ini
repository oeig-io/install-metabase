# inventory.ini - Ansible inventory for Metabase servers
#
# For local installation (same machine):
#   ansible-playbook -i inventory.ini metabase-install.yml --connection=local
#
# For remote installation:
#   Update the IP address and SSH settings below

[metabase_servers]
# Local installation
localhost ansible_connection=local

# Remote installation (uncomment and configure)
# metabase-prod ansible_host=192.168.1.100 ansible_user=root
# metabase-staging ansible_host=192.168.1.101 ansible_user=root

[metabase_servers:vars]
# Common variables for all servers (can be overridden per host)
ansible_python_interpreter=/run/current-system/sw/bin/python3
